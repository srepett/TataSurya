
export interface PlanetData {
  name: string;
  color: string;
  size: number; // Radius
  distance: number; // Distance from the sun
  orbitalSpeed: number; // Speed of orbit around the sun
  rotationSpeed: number; // Speed of self-rotation
}
